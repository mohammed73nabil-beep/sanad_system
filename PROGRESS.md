# SANAD | سَنَد — سجل الإنجاز والتقدم

> **الحالة:** مكتمل بالكامل وجاهز للتشغيل والإنتاج ✅
> **تاريخ التحديث:** 2026-08-27

---

## 🌟 نظرة عامة على ما تم إنجازه

تم بناء نظام **سَنَد | SANAD** بالكامل وفق أعلى معايير الجودة والأداء، بهوية بصرية مميزة (أزرق كحلي `#1B4B6B` + ذهبي `#C8922A`)، وخط **Cairo** العربي، ودعم كامل للغة العربية واتجاه RTL.

---

## ✅ المراحل المنجزة:

### 1. البيئة والتأسيس (Environment & Infrastructure)
- [x] إعداد قاعدة بيانات MySQL `sanad_system_db` بدقة مالية `DECIMAL(15,2)` لجميع الحسابات
- [x] تثبيت وضبط Inertia.js React مع Laravel 12
- [x] إعداد Tailwind CSS و PostCSS ونظام الألوان الموحد
- [x] إنشاء `routes/web.php` الكامل لجميع الأقسام و `routes/api.php` المحجوز للجوال

### 2. قاعدة البيانات والنماذج (Migrations & Models)
- [x] 15 جدول مهجر ومفهرس بالكامل (`users`, `company_settings`, `categories`, `units`, `customers`, `suppliers`, `products`, `purchases`, `purchase_items`, `invoices`, `invoice_items`, `payments`, `receipt_vouchers`, `inventory_movements`, `attachments`, `audit_logs`)
- [x] ضبط `invoices.type` ليكون `sale` فقط (فصل الشراء في جدول `purchases`)
- [x] العلاقات الكاملة في الـ Eloquent Models مع Scopes و Accessors

### 3. الخدمات الخلفية (Core Service Layer)
- [x] `NumberGeneratorService`: توليد أرقام تسلسلية فريدة (`INV-000001`, `PUR-000001`, `RCV-000001`)
- [x] `TaxService`: احتساب الضريبة بدقة وفق النسبة المحددة للمنشأة
- [x] `InventoryService`: حركة المخزون الذاتية وتحديث الأرصدة ومنع الرصيد السالب
- [x] `PaymentService`: إدارة الدفعات وإعادة احتساب الأرصدة المتبقية وتحديث حالات الفواتير
- [x] `ReceiptService`: إنشاء سندات القبض الآلية مع التفقيط الكامل للمبالغ باللغة العربية
- [x] `QrCodeService`: توليد بيانات الـ QR المشفرة (ZATCA Phase 1 TLV Base64)
- [x] `InvoiceService`: دورة حياة الفاتورة من الإنشاء حتى الإلغاء مع المعاملات الذرية `DB::transaction()`
- [x] `PurchaseService`: تسجيل مشتريات الموردين وتحديث المخزون التلقائي
- [x] `ReportService`: تقارير المبيعات، المشتريات، الإقرار الضريبي، كشوف الحسابات، والأرباح
- [x] `PdfService`: توليد مستندات PDF احترافية قابلة للطباعة

### 4. واجهات المستخدم والتفاعل (Frontend React Pages)
- [x] **المصادقة (Auth):** صفحة تسجيل الدخول `Pages/Auth/Login.jsx`
- [x] **لوحة التحكم (Dashboard):** مؤشرات الأداء الحية، الرسوم البيانية لآخر 7 أيام، تنبيهات المخزون، وقوائم المتابعة
- [x] **العملاء والمحلات (Customers):** إدارة العملاء، فلاتر المديونية، كشف حساب العميل التراكمي مع تصدير PDF
- [x] **الموردون (Suppliers):** إدارة بيانات الموردين ومتابعة مستحقاتهم
- [x] **التصنيفات والمنتجات (Products & Inventory):** إدارة الكتالوج، الباركود، SKU، والحد الأدنى للمخزون
- [x] **فواتير المبيعات (Invoices):** منشئ الفواتير التفاعلي السريع مع الحساب المباشر وسداد الدفعات والطباعة وPDF
- [x] **فواتير المشتريات (Purchases):** تسجيل فواتير الشراء، رفع مرفقات فواتير الموردين، وزيادة المخزون
- [x] **سندات القبض (Receipt Vouchers):** استعراض وإصدار السندات مع التفقيط والطباعة وPDF
- [x] **حركة المخزون (Movements):** سجل تدقيق الحركات قبل وبعد كل عملية
- [x] **ملف المحاسب والتقارير (Accountant Pack & Reports):** تقارير تفصيلية شاملة مع تصدير PDF و Excel (CSV)
- [x] **إعدادات المنشأة (Settings):** تعديل بيانات المؤسسة، الرقم الضريبي، نسب الضرائب، ورفع الشعار

### 5. مستندات الطباعة و PDF
- [x] `pdf/invoice.blade.php`: فاتورة ضريبية A4
- [x] `pdf/invoice_print.blade.php`: طباعة مباشرة من المتصفح
- [x] `pdf/receipt_voucher.blade.php`: سند قبض معتمد
- [x] `pdf/customer_statement.blade.php`: كشف حساب عميل
- [x] `pdf/accountant_report.blade.php`: ملف المحاسب والضريبة

---

## 🔑 بيانات الدخول الافتراضية للتجربة:

- **البريد الإلكتروني:** `admin@sanad.sa`
- **كلمة المرور:** `password`

---

## 🚀 تشغيل النظام محلياً:

1. تشغيل خادم Laravel:
```bash
php artisan serve
```

2. تشغيل خادم تطوير Vite (في حال التعديل على الواجهات):
```bash
npm run dev
```
*(أو الاعتماد على الحزم المبنية مسبقاً عبر `public/build`)*
